<!DOCTYPE html>
<html lang="en">
<head>
    <title>Events</title>
    {% include 'commonHeader.twig' %}
</head>
<body>
{% include 'navbar.twig' %}
{% import 'macro/playerMacro.twig' as pm %}
<div class="container">
    <div class="row my-row">
        <div class="col-sm-12">
            <table class="my-table table-bordered table-striped events-table">
                {% for event in events %}
                    <tr>
                        <td>{{ pm.eventLink(event) }}</td>
                        <td>{{ event.date }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>
</div>

</body>
</html>
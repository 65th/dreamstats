<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{ player.nickname }} & {{ ally.nickname }}</title>
    {% include 'commonHeader.twig' %}
</head>
<body>
{% include 'navbar.twig' %}
{% import 'macro/playerMacro.twig' as pm %}

<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <p>
                {{ pm.player(player) }} & {{ pm.player(ally) }}
            </p>
        </div>
    </div>
    <div class="row my-row">
        <div class="col-sm-12">
            {% if atMatches %}
                {% include 'atMatchesTable.twig' %}
            {% else %}
                <h1>Некомандный игрок!</h1>
            {% endif %}
        </div>
    </div>
</div>
</body>
</html>